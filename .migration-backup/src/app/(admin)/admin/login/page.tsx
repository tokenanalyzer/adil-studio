export default function AdminLoginPage() {
  return (
    <main className="container" style={{ padding: "64px 0" }}>
      <h1 style={{ fontSize: "40px", marginBottom: "16px" }}>Admin Login</h1>
      <p style={{ color: "var(--muted)", maxWidth: "720px" }}>
        Phase 2 auth shell is ready. Supabase magic link / OTP form will be wired in the next phase.
      </p>
    </main>
  );
}
